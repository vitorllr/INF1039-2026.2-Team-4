# Project agent memory

This file is the project's committed home for project-intrinsic agent knowledge: build, test, release, architecture, and sharp-edge notes that should travel with the code.

- Add durable project-specific notes here as they are discovered through real work.
- App codename: BlueTide (flood/tide alert app combining the INEA rain/tide bulletin with rainfall and tide data into a green/amber/red recommendation). `main.py` at the repo root is an unrelated leftover calculator stub, not part of the app.
- Deliverables live under `Sprint-N/<Nome-Sobrenome>/`, one folder per sprint per teammate. No app scaffolding exists yet; deliverables are mostly documents (PDFs, Markdown).
- Visual identity and logo concepts are in `Sprint-3/Vitor-Rocha/identidade-visual/` (palette, typography, and 5 logo SVG concepts with the chosen one explained).
- Every PR in this repo must be passed through the `humanizer:humanizer` skill on its prose (Markdown deliverables and PR description) before opening it.

## Maintaining this file

Keep this file for knowledge useful to almost every future agent session in this project.
Do not repeat what the codebase already shows; point to the authoritative file or command instead.
Prefer rewriting or pruning existing entries over appending new ones.
When updating this file, preserve this bar for all agents and keep entries concise.
